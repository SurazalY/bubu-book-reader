---
description: 派发子 agent 时的模型选用惯例（用户定版，硬约束）
alwaysApply: true
---

# 派发子 agent 的模型选用

派发子 agent 时**必须显式指定模型**。该参数可选，漏填会落到默认模型上——本项目已因此出错两次。

## 三条硬约束

- **默认一律用 grok**，当前可用的是 `cursor-grok-4.6-xhigh-fast`。不分任务轻重，小活也用它。
- **`composer-2.5-fast` 永远不启用。** 用户判定其能力不足。这是禁令，不是偏好。
- **opus（`claude-opus-5-thinking-xhigh`）只在用户当场明确指定时才用。** 用户要求尽量减少对其依赖。

不要为省钱把小任务降到更弱的档：**grok 的实际成本低于 gemini**，降档没有收益，只降质量。

模型可用列表会变，派发前先确认当前环境有哪些。历史文档里出现的 `cursor-grok-4.6-high-fast` 已不存在。

## 高风险任务怎么办

项目早期文档（`docs/product-close-loop/03_实施任务清单.md`、`05_主控交接说明.md`）曾把某些任务点名为"高风险设计类、须用 opus"。**该要求已于 2026-08-18 被推翻。** 若在旧文档里读到它，按本规则执行。

替代做法是**把风险机械化**，两步：

1. 把设计决策提前写死进契约文档（表结构、字段、端点语义、禁令），让子 agent 拿到的是执行任务而非设计任务。
2. **先派一个独立 agent 写守卫测试，再派另一个 agent 写实现**，并在实施方的简报里写明"不得修改那道守卫测试"。守卫变红一律按实施越界处理，不得靠改测试消红。

依据是本项目的实证：Phase 4 最危险的缺陷 F-1（越权覆盖全组织授权）是独立的对抗式守卫 agent 攻出来的，写实现的 agent 自己没发现、主控抽查也没发现；D-10 那次子 agent 留下一个默认分支把已修缺陷改成休眠状态，抓住它的是人读源码。**两次都不是模型档位能解决的问题。预算花在独立守卫与抽查上，回报高于花在档位上。**
