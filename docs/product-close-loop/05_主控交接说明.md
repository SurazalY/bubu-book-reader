# 主控交接说明（Handover）

本文件由前一任主控 Agent 在 2026-08-17 Phase 3 进行中写就，用于把无法从代码里读出来的东西交给继任者：**已经踩过的坑、已经作出的裁决、以及为什么某些做法不能变**。

## 0. 使用契约（先读这一节）

1. **每一任主控接手时必须整篇读完本文件**，再读 `README.md` → `01` → `02` → `03` → `04`。本文件优先级高于你对代码的直觉判断。
2. **本文件只允许追加，禁止删改或"整理精简"既有内容。** 你觉得某条过时了，就在末尾追加一条说明它为何过时，不要把原文删掉。历史结论被删是信息丢失的主要方式。
3. **交出去之前，必须在第 10 节追加一条交接记录。** 不得用"详见对话历史"代替文字——对话上下文不会传给你的继任者，只有仓库里的文件会。
4. 如果你的上下文被压缩过（长会话必然发生），**在写交接记录前重读本文件全文**，不要凭压缩后的记忆写。

## 1. 当前进度与检查点

分支 `feat/product-close-loop`，不要 push、不要建 PR，除非用户明确要求。

| 检查点 | 内容 |
|---|---|
| `f783a8c` | Phase 0 基线 |
| `de04ae0` | 固化接手前遗留基线（双模式阅读器、逐页覆盖计时、受保护资产接口、AI 选词） |
| `70a3c36` | Phase 1 编目 + trusted 打包器 + 导入链 trusted 模式 |
| `eae6612` | 修原版 PDF 无法加载（pdfjs 改 legacy 构建）、封面 400、dev 公开路径泄露 |
| `937ce0b` | 忽略 10.4 GB 设备迁移归档目录 |
| `f5f99a8` | 修计时汇总在首次提交后永久停摆（D-11） |
| `8e25247` | 隐藏三维翻页并建技术债档案（D-03，见 `docs/tech-debt.md`） |
| `6b61aa7` / `22164d2` | 确定化 coordinator 测试竞态（定性为测试侧，非产品缺陷） |
| `d322cfd` / `66a34be` | 受保护资产消费方案定档（D-09/D-10） |
| `a2c1ed3` | 服务端拒绝为停滞会话续租，解除僵尸租约自锁（D-15） |
| `fcdccfe` | 展示阅读进度的页面挂载时后台刷新 runtime（D-16） |
| `da15acc` | 归档 D-16 与计时归因核查，定档 Phase 6 两条裁决 |
| `c849707` | **T3.1 批量打包完成：49/49 成功** |
| `c0dfdaf` | 从结构上消除脱管客户端锁死租约（D-17，三层：无会话不得续租 / 活跃阅读不得被抢 / 导航 4s 有界超时） |
| `a4d15ea` | 更正脱管标签页教训，归档会话密钥轮换应急开关 |
| `c40765a` | 归档 D-17 真人复验通过与 B1 第二次自动生效证据 |

**质量门基线（每个 Phase 收尾必须全绿）**：`npm run test:server` = **207/207**，`npm run test:frontend` = **179/179**，`npm run build` 退出码 0。你新增用例后数字变大属正常，变小要查。

> 基线是随修复增长的，别拿更早文档里的旧数字对照：Phase 2 收尾时是 200/177，D-17 新增 9 条用例后变成 207/179。

`tests/frontend/reading-monitor-client-coordinator.test.mjs` 的历史抖动已全部确定化。**该文件今后再失败就是真回归，不许靠重跑掩盖。**

## 2. 你的授权范围

如果你被指派的是 **Phase 3**，那么你只做 Phase 3，做完就交给更强的继任者。**不要顺手去做 Phase 4/5/6/7**——Phase 4 的权限边界与 Phase 7 的验收报告已明确交由能力更强的 agent 承接，提前动手会留下需要返工的半成品。

Phase 3 已完成 T3.1（打包），**剩 T3.2 批量导入、T3.3 全量核对、T3.4 真人抽查**。

**开工前状态（2026-08-17 19:00 前一任实测，可直接开工，无阻塞项）**：

- 质量门最后一次全绿在 `c0dfdaf`：server 207/207、frontend 179/179、build 退出码 0。其后两个提交（`a4d15ea`、`c40765a`）只动文档与证据，**无需重跑即可开工**；Phase 3 收尾照常要跑。
- 工作树干净（仅 `.trellis/`、`AGENTS.md` 未跟踪，属工具目录，不要提交）。
- 磁盘 D 盘可用 228.7 GB。包产物已占 3.36 GB；`public/books/` 当前 0.1 GB（只有 book-001），全量导入后约 3.4 GB。
- **关于 build 耗时的准确预期**（这条容易让人误判）：`npm run build` 就是 `vite build`，而 `vite.config.js` 未覆盖 `publicDir`/`outDir`，因此走默认行为——**把整个 `public/` 复制进 `dist/`**。导入前 `public/books/` 只有 0.1 GB，build 约 6 秒；**导入 49 本之后要复制约 3.4 GB，Phase 3 收尾那次 build 会明显变慢（约 1 分钟量级）。看到从 6 秒变成 1 分钟不要以为卡死或改坏了，这是数据量导致的。**
- **Phase 2 已全部闭环**：D-16（详情页返回后免刷新更新）与 D-17（租约三层根治）均已真人复验通过，见台账第十一、十四节。开工无阻塞项。

## 3. T3.2 批量导入：必须知道的陷阱

这一节是前一任写好但尚未执行的任务要点，逐条都有代价背书，不要跳过。

### 3.1 先备份，且不能用文件复制

数据库是 **WAL 模式**，未 checkpoint 的数据在 `-wal` 文件里，**单独复制 `.sqlite` 不是有效备份**。用 `VACUUM INTO '<路径>'` 或 SQLite 备份 API。备份后要验证能打开且关键表行数合理。命名参照 `server/data/` 下已有的 `readmate.sqlite.phase0-backup`、`readmate.sqlite.pre-phase2-backup`。

### 3.2 book-001 上挂着真人的真实阅读数据 —— 已裁决：T3.2 跳过它，只导另外 48 本

**库状态已于 2026-08-17 19:00 只读核验（前一任亲自查的，不是推测）**：`books` 表只有 1 行 `book-001`（`published`），`book_versions` 只有 `book-001-trusted-v1`（`trusted-baseline`）。**book-028 并未导入**，之前"可能已导入"的说法是错的。所以 T3.2 的实际输入是 **48 本全新导入 + book-001 跳过**。

真人的阅读记录通过 `book_version_id = book-001-trusted-v1` 引用 book-001（`reading_summary_sessions` 7 行、`reading_page_coverage`、`reading_progress`、`reading_daily_book_summaries`），**重导它是全 Phase 3 唯一有数据事故风险的动作**。

**裁决：跳过 book-001，不要重导。** 依据是它当前入库状态已满足 T3.3 的全部五类断言（同一次只读核验确认）：

| T3.3 断言 | book-001 实测 |
|---|---|
| `published` | ✓ |
| `book_pages` 行数 = pageCount | 98 = 98 ✓ |
| blank 页 `normalized_text` 为空 | 10 行空文本，与包内 blankPageCount=10 一致 ✓ |
| 恰 1 个 `source_pdf` + 1 个 `cover` | `book_assets` 2 行，类型正确 ✓ |
| `grade` 齐全 | `book_catalog_metadata.grade = 1` ✓ |

在导入汇总表里把 book-001 记为"已存在且已核验合规，按裁决跳过"，T3.3 照常把它纳入断言范围（只读，安全）。

**若你出于任何理由认为必须重导 book-001，那是需要先问用户的操作**，且必须先查清：导入器 `server/db/import-book-package-v2.js` 对已存在的 book / book_version 是幂等 upsert 还是先删后建？若存在删除 `book_versions` / `book_pages` 行的路径，是否会经外键级联或触发器波及 `reading_*` 表？查不清就不要动。

无论是否跳过，导入前后都用下面这组值做一次前后对照并写进报告（任何一项变化都说明动到了真人数据）：

| 表 | 导入前的值 |
|---|---|
| `reading_summary_sessions` | 7 行，`cumulative_effective_ms` = 194544 / 1480 / 20651 / 3978 / 24888 / 19184 / 97821 |
| `reading_progress` | 1 行，`last_page_no` = 21 |
| `reading_daily_book_summaries` | `stat_date=2026-08-17` 合计 362546 ms |
| `reading_page_coverage` | 22 行 |

上表是 2026-08-17 19:00 的快照（当时 7 行会话、`last_page_no=21` 已复核仍成立）。**用户若在你开工前复验 D-16，就会真实阅读，这些数值必然变大——那是正常的，不是事故。** 所以你要做的是**在自己动手导入前重新采样一次**作为基准，别拿上表当硬断言。判据是"导入前后你自己采的两次相同"，不是"和上表相同"。

### 3.3 book-001 / book-028 的包是复用的，先验确定性

T3.1 汇总里 `skippedExisting = true` 的恰是这两本（book-001、book-028），复用了 Phase 1 的旧产物，未用当前打包器重建。若当前打包器产出与旧产物不同，那么 book-028 会以一个"旧格式包"被导进库，而 book-001 的入库内容与当前打包器输出不一致。

先用 `--force` 重建这两本（**打包只写包目录、不碰数据库，安全**），把新的 `manifestSha256` 与 T3.1 汇总比对：

- book-001 应为 `05941212b7f9cf33b6d7d52042c41cfb9bfad523e97c676fe96e502a902842d6`
- book-028 应为 `e0010463288f24fb31d2e595c14151b831404e1df9de6f4ca9f5e697b67b6570`

book-028 若不一致，用重建后的新包导入即可（它还没入库，无风险）。**book-001 若不一致，停下来报告用户，不要自行决定重导**——见 §3.2 裁决。

### 3.4 其余要点

- 导入必须带 `--accept-trusted`（决策 D1：OCR 是可信输入）。所有包 `quality.status = trusted-baseline`。
- **book-031 导入前再断言 `pageNo` 1–161 连续**（T3.1 已确认，仍按清单要求复验）。
- **npm 11 会吞掉 `--` 后的参数**，直接用 `node server/db/import-book-package-v2.js ...`，不要用 `npm run xxx -- --flag`。
- `--public-root` 必须与运行时 `PUBLIC_ASSET_DIR` 一致。`.env` 里配的是 `PUBLIC_ASSET_DIR=../public`（注意：`server/app.js` 的代码默认值会退到 `dist/`，与 README 早前写的 `public/` 不一致，这条踩过）。
- **逐本独立、失败续跑**：某本失败不中断整批，记录原因继续，最后如实列出成功/失败。
- **不得对 OCR 做任何质量验收或修正。** 不要因为某页文本"看起来不对"就改它或报缺陷。
- 磁盘：包产物已占 3.36 GB，导入会往 `public/books/` 复制，`npm run build` 还会往 `dist/` 复制约 3.3 GB。D 盘当时可用 231 GB，够用但要留意。
- **导入期间不要让任何人在浏览器里阅读**：WAL 模式下读不受阻，但 `busy_timeout = 0`，服务端写入（摘要提交、租约获取/续期）撞上导入写锁会立刻报 busy 而不是排队等，看起来像一次莫名的提交失败。交接时前后端都在运行中（后端 5191、前端 5190，pid 以当时为准），**导入前先跟用户确认他已关闭所有指向本应用的浏览器页面**；后端进程本身可以留着（空闲时不写库），若导入器报 `SQLITE_BUSY` 再请用户停后端后重跑该本。

## 4. T3.3 全量核对

写一个**只读**核对脚本（可参照 T3.1 的编排脚本 `book-parser/pipeline/batch_build_trusted_package_v2.py` 的组织方式：编排层复用既有逻辑，不重复实现）。五类断言：

1. 49 本 books 均 `published`
2. 每本 `book_pages` 行数 = `pageCount`
3. blank 页 `normalized_text` 为空
4. 每本恰有 1 个 `source_pdf` + 1 个 `cover` 资产
5. `grade` 齐全

汇总表存入 `docs/product-close-loop/evidence/phase3/`。

## 5. T3.4 真人抽查：由用户做，不要自己开浏览器

用户明确要求过：**agent 不要操作浏览器，真人验收由他本人做**。你的职责是给出最短、可执行的验证步骤，然后等他的反馈。

抽查建议（来自 03 文档 T3.4）：学生书架 49 本全部可见；抽 book-005（嵌套 source.json）、book-031（补页书）、book-045（最厚 354 页）打开验证双模式与补入页内容正常。

**顺带请他留意一个未定性的观察项**：原版 PDF 模式下 book-001 右页曾整页橙色，当时无法判断是该书印刷底色还是渲染问题。若换本书也出现，就是渲染缺陷；只有 book-001 第 2 页这样，就是印刷底色，不记缺陷。

## 6. 不可协商的硬规则

每条都带代价，不是形式主义。

1. **不得修改阅读计时的摘要 schema 与指纹算法**：`src/student/reading-monitor/summary.js` 的 `FINGERPRINT_FIELDS`（第 3 行起）与 `createSummaryRevision()`（第 42 行起）。项目硬契约 B-2，见 `02_决策与契约边界.md`。
2. **不得新建第二套计时/进度/发布系统。** 工作量主体是接入与补缺，不是重写。
3. **不得对 OCR 做质量验收。** OCR 是可信输入。
4. **不得擅自向业务库 `server/data/readmate.sqlite` 写入数据。** 任何会删除/覆盖既有阅读记录或归档数据的操作，先问用户。需要可写库就复制副本到临时目录。
5. **不得改动 `device-migration-20260815/` 下的归档内容**；`book-parser/work/ocr-antigravity-v1/` 是只读输入。
6. **服务端改完必须重启后端，Node 不热加载**；而且**重启+单元测试全绿仍不足以宣称生效**，必须在真实环境观测到预期行为（见第 9 节教训 1 和 2）。
7. **派发子 agent 时必须显式指定模型。** 该参数是可选的，漏填会落到默认模型上——前一任犯过两次。

   **模型选用惯例（2026-08-18 由用户重新定版，取代此前的写法）：**
   - **默认一律用 grok**（当前可用的是 `cursor-grok-4.6-xhigh-fast`），不分任务轻重。用户已说明 grok 的实际成本低于 gemini，所以**不需要为省钱去挑更弱的档**。
   - **永远不要启用 `composer-2.5-fast`**——用户明确判定其能力不足。这是硬禁令，不是偏好。
   - **opus 只在用户明确指定时才用。** 用户要求尽量减少对其依赖。03 与 05 文档早期把 T4.3、T5.1 点名为"高风险设计类、须用 opus"，**该要求已被推翻**：那类任务的设计风险应当靠"把设计写死在契约文档里 + 独立 agent 写机械闸门"来消除，而不是靠抬高模型档位。依据见下面一段。
   - 模型可用列表会变，派发前先确认当前环境有哪些。

   **为什么闸门比档位有效（Phase 4 的实证）：** 该阶段最危险的缺陷 F-1（班级级教师用 `scope=organization` 绕过班级授权、反向覆盖全组织授权）是**独立的对抗式守卫 agent 攻出来的**——写实现的 agent 自己没发现，主控抽查也没发现。D-10 那次子 agent 留下 `max-age=3600` 默认分支、把已修缺陷改成休眠状态，抓住它的是人读源码。两次都不是模型档位能解决的问题。**结论：预算花在独立守卫与抽查上，回报高于花在档位上。**
8. **子 agent 之间不共享上下文**，每次派发必须给自包含简报：背景、目标文件路径、契约边界原文摘录、完成标准、要求返回的报告格式。涉及 B-2 禁令的任务必须把禁令原文附上。
9. **不盲信交付。** 关键改动做最小抽查（读单个文件、跑单条只读查询这个量级），必要时再派一个 agent 复核。
10. **未经真实运行不得宣称验收通过**，最终报告按 04 文档五级如实标注。

## 7. 未关闭的缺陷与风险

完整台账在 `docs/product-close-loop/evidence/phase2/defect-ledger.md`（第一节是清单，第七节 D-11 专项、第十节 D-15 专项、第十一节 D-16 专项、第十二节计时归因核查、第十三节 Phase 6 裁决与 K-01~K-06 已知偏差）。**下面只是索引，细节以台账为准，不要用这份摘要替代台账。**

| 编号 | 一句话 | 归属 |
|---|---|---|
| D-06 | 文字模式长页内容截断（已定性应修，修法需设计取舍） | Phase 5 |
| D-09 / D-10 | 受保护资产消费：方案已定档（路线 1 前端统一 fetch+blob；封面 no-store、PDF no-cache+ETag），待落地 | Phase 4 |
| D-12 | 跨设备真冲突时 `LEASE_CONFLICT` 丢弃队头、本地 revision 已 commit，下次变 `REVISION_GAP`，**整段时长静默丢失**（不报错、不可见，这是最危险的部分） | 用户 2026-08-17 明确表态暂无跨设备需求、无从验证，故继续挂起，不阻塞任何 Phase。**但一旦将来引入多设备场景，必须先修** |
| D-13 | `otherOpen` 用 `.get()` 且未滤 `workspace_id`，多条残留 open 一次只关一条 | 建议项 |
| D-14 | `getBookAsset` 未按发布状态与班级可见范围过滤，同工作空间内任何登录用户可取任意书资产 | **Phase 4 核心前置** |
| ~~D-16~~ | 详情页返回后进度不刷新 | **已修 `fcdccfe` 并真人复验通过**（用户原话「不需要刷新了，现在时长和页面的同步是实时的」） |
| ~~结构隐患~~ | 导航依赖无超时的 `waitForTerminal` | **已随 D-17 修复**（4 秒有界超时 + try/finally），检查点 `c0dfdaf` |
| D-17 残余 | 活着的脱管客户端仍可反复占用 ≤420 秒的租约窗口。**不能靠缩短阈值解决**：会话行只在首次摘要提交时落库、而首次提交在第 5 分钟 tick，故存在 300 秒内「正当但尚无会话」的盲窗，阈值必须大于它。彻底消除需让「无会话」状态不可能存在（取得租约时即建会话行），但那贴近禁改的摘要修订与指纹链，属高风险设计 | 已接受为残余风险，需要时再单独立项 |
| 观察项 | 原版模式右页整页橙色，未定性 | 见第 5 节 |

**Phase 5 的实际缺口**（前一任在 2026-08-17 核对过，不要误以为已完成）：T5.1 模式持久化**未做**（全仓搜 `reader-preference` 只在计划文档里出现）；T5.2 空白页占位**未做**；T5.3 跳页参数**未修**，`?page=` 仍存在于 `src/student/pages/Highlights.jsx` 两处与 `src/student/pages/PostDetail.jsx` 一处（Reader 认的是 `?pageNo=`）；T5.4 学生端退出登录**未做**；T5.5 印刷页标签已实质通过（本批 49 本 `printedPageLabel` 全为 null，底栏物理页语义正常）。

## 8. 交给更强继任者的两段

### Phase 4（最容易出错的一段）

T4.3 要求学生角色在 **`listBooks` / `getPage` / `getBookAsset` / AI 书籍访问四个入口统一按班级可见范围过滤，不可见返回 404**。四处漏一处就是越权，而 D-14 已确认 `getBookAsset` 目前完全没有这层过滤。叠加 D-09/D-10 的资产消费改造（要把所有图片展示位改成 fetch + blob，涉及详情页、教师端书库、社区帖图多处），这是全程风险最高的部分。落地时需要一条**守卫测试防止新增图片展示位绕过共享 hook**（不能只靠约定），并实测 `no-cache` 下 pdf.js range 请求是否走 304 而非整本重传。

### Phase 7

需要真实 AI 凭据（`AI_BASE_URL` / `AI_API_KEY` / `AI_MODEL` 写入 `.env`），届时向用户索取，凭据不入 Git。`INTERNAL_DEMO_PASSWORD`（≥12 位）需记录在报告中但不入 Git。最终报告按 04 文档模板写到 `docs/product-close-loop/最终验收报告.md`，**每条按 L1–L5 如实标注，未经真实运行验证的项不得宣称通过**——这是用户反复强调的红线。

Phase 6 已定两条裁决（见台账第十三节）：D-12 不阻塞、按单设备单标签验收；库中那条僵尸坏会话（194544 ms）不清理，**改用增量口径验收**——读 X 分钟看总时长是否增加 X，增量对该污染免疫。

## 9. 用血换来的教训

1. **服务端改完不重启等于没改。** Node 不热加载。曾因此让用户白读 6 分钟。
2. **单元测试全绿不等于线上生效。** D-15 的首版修复在停滞判定 SQL 里带了 `status = 'open'` 过滤，而僵尸页重新获取租约会把会话标成 `closed`，判定永远查不到行。server 196/196 全绿，线上判定从未触发。是去数据库看续期计数才发现的。**回归用例必须按真实生产路径建模，不能只测纯函数或组件内部实现。**
3. **agent 自己开的浏览器标签页会脱管，并持续占用业务资源。** 一个下午遗留的阅读器页面每 60 秒续租一次，把租约钉死近三小时，导致真人连监测会话都建不起来，三轮复验全败。**验证完必须关页。**

   **2026-08-17 19:41 更正，这条比原先写的更严重**：该标签页**在用户的 Cursor 界面里根本看不到、也无法关闭**（用户原话"这个根本就没有啊，至少对我是透明的"）。`browser_tabs list` 在主控会话里返回空，说明它属于别的 agent 会话的浏览器上下文，已完全脱管。**结论：「用完关页」不能作为可依赖的防线，因为人类没有执行它的手段。** 因此不要把任何缺陷的收尾寄托在"让用户去关那个页面"上。

   **可复用的应急手段（本次实测有效）**：轮换 `.env` 里的 `SESSION_TOKEN_SECRET` 并重启后端，即可让所有既有会话签名失效——脱管页面从此无法认证，既不能续租也不能重新获取租约，且**完全不触碰业务数据**（实测四张阅读事实表前后一致）。代价仅是所有人需重新登录一次，演示密码不受影响。比去猜哪个进程该杀安全得多。
4. **排查"客户端仍在活动"要先用连接归属证据定宿主进程，别先让真人关浏览器。** 当时误判为"用户 Chrome 后台残留"，用户白关三次、杀掉 29 个 chrome.exe 仍无效。真凶是 Cursor 内嵌浏览器里的页面。有效手法：`Get-NetTCPConnection` 观察前后端端口的客户端端口是否连号成对，再查 socket 归属进程及其父进程。
5. **主控别自己钻代码。** 前一任被用户当场指出过"你怎么一直在自己探索"。允许的直接操作只有读计划文档、git 检查点、单文件/单条只读查询级别的抽查。
6. **裁决要留痕。** 所有决策写进台账或计划文档，不要只存在于对话里。

## 10. 交接记录（只允许追加）

### 2026-08-17 · 第一任主控 → Phase 3 承接者

- **交出时 HEAD**：`c40765a`（Phase 2 全部闭环，Phase 3 完成 T3.1）
- **本任完成**：Phase 0/1/2 全部，Phase 3 的 T3.1；期间修复 D-03（隐藏）、D-04、D-05、D-11、D-15、D-16、D-17 与三条 Phase 2 缺陷；建立缺陷台账、两份设计定档与本交接文档
- **验证方式（逐项如实标注）**：
  - Phase 2 主干、D-16、D-17 的 B3 层：**真人实测通过**（时长为分钟级主观口径，非秒级精确验证）
  - D-15、D-17 的 B1 层：**真实环境实测**（观测到 409 拒续、version 冻结、租约自然过期，且既有阅读数据未变）。D-17 的 B1 另有一次全自动开火证据
  - D-17 的 B2 层（活跃阅读不得被抢）：**仅自动测试覆盖，未经真实双设备实测**，按 L2 记
- **明确没验的**：D-17 的 B2；Phase 5 五项中的四项（T5.5 已实质通过）；Phase 4 全部；原版模式右页整页橙色观察项；D-12 跨设备场景（用户暂无该需求）
- **移交时未提交的东西**：无（`.trellis/` 与 `AGENTS.md` 为工具目录，非本任产出）
- **本任对环境做过的一次不可逆操作**：轮换了 `.env` 的 `SESSION_TOKEN_SECRET`（为断开一个用户无法关闭的脱管页面）。**后果：所有旧登录态失效，任何人再登录都要走 `/student/login`。** 演示账号 `internal-student`，密码见 `.env` 的 `INTERNAL_DEMO_PASSWORD`，未改动。
- **给承接者的三句话**：
  1. T3.2 唯一真正危险的动作是重导 book-001，真人阅读数据挂在它的 version 上——已裁决跳过，别自己改主意。
  2. 遇到「返回键点不动 / 时长不记录」不要重新从零诊断，先读台账第七、十、十四节，那是同一结构三次复发的完整病历。
  3. 子 agent 报「测试全绿、修复完成」不等于生效。本任最有价值的一次判断就是拒绝采信绿灯、去数据库看了一眼续期计数。

### 2026-08-17 深夜 · D-19 执行者 → Phase 4 承接者

- **交出时 HEAD**：`063f1bc`（T3.2 导入 48 本 + T3.3 全量核对完成）。**工作树里 D-18 与 D-19 的成果全部未提交**，禁止用 `git checkout` / `reset` / `stash` / `clean` 丢弃。
- **本轮完成**：D-19（原版 PDF 正文发糊）定性与修复。台账新增第十五节专项。**未做 Phase 4 的任何代码**，只为它写了交接文档。
- **给 Phase 4 承接者的入口**：新增 `06_Phase4交接说明.md`。**那份文件里含四处对既有文档的更正**，其中两条会直接害人，请务必先读：
  1. 台账 D-14 写「`getBookAsset` 未按发布状态过滤」——**这半句是错的**，`book.status = 'published'` 一直在（`catalog.js` 185–198 行）。它真正缺的只有班级 grants，与另外三个入口缺的是同一层。
  2. Phase 4 完成标准要求「用 `POST /classes` 建第二个班验证」，但该路由要 **school 范围**的 `class.manage`，而演示数据里两位老师是 **class 范围**。**必须用校长账号 `internal-principal` 建班**，别等到验收当天才发现建不了。
  3. `02` 文档写「新迁移从 045 起」已过时，045 被 Phase 1 用掉了，**从 046 起**。
  4. `permissions.js` 里**没有 `class_teacher` 角色**，只有 student / teacher / grade_manager / school_admin / platform_ops，T4.2 不需要处理别名。
- **质量门（本轮实跑）**：`test:server` 207/207、`test:frontend` **194/194**（第 1 节写的 179 已过时，D-18 与 D-19 新增用例所致）、`build` 退出码 0。**`build` 实测 8.71 秒，不是本文件第 2 节预期的「1 分钟量级」**——那条预期没有成立，`dist/books/pilot` 49 本齐全，属于虚惊。
- **验证方式（逐项如实标注）**：
  - D-19 的**定性**：实测。用 PyMuPDF 只读量了产品里的 `source.pdf`（页尺寸、174 DPI、扫描图像素、汉字字身），并逐像素量了用户提供的两张真实截图；推出的恒等式 `汉字设备像素 = 字身pt × 舞台物理像素高 ÷ 页高pt` 与截图实测吻合到 0.2%。
  - D-19 的**修复**：**同夜真人复验通过**（用户原话「清晰度基本没问题了」），按 L4 记，但仅覆盖抽样观感——未逐本点验 49 本，三处改动也无法归因到单项。修的是「把空转的『大』字号档改成真正放大 1.6 倍 + 纵向滚动」「上下栏收起时真正退出 flex 流」「canvas CSS 尺寸取整」。
  - 交接文档里的事实：附录逐条标注了哪些是我读源码核实的、哪些是子 agent 探查后我只抽查了关键部分的。
- **明确没验的**：上下栏收起实际释放多少像素（按 CSS 盒模型算的，未实测栏高）；用户屏幕的 dpr 与分辨率真值（用户未跑控制台脚本，我是从「F11 舞台 849 + 上下栏约 231 = 1080」反推的）；49 本逐本观感。**D-19 的整体观感已由用户当夜复验通过**，不在此列。
- **本轮对环境做过的不可逆操作**：无。没写数据库、没重导、没提交、没 push、没开浏览器。
- **一条方法论留痕**：D-19 之所以能定性，靠的是**先算术、再实验、最后才改代码**。前两轮（D-18 选尺、提高 canvas backing）都是先改后看，各花掉一轮却都没碰到根因——因为它们改的量根本不在那条恒等式里。**遇到「玄学发糊 / 玄学变慢」这类问题，先把物理上限算出来，能省掉一半假设。**

### 2026-08-18 · Phase 3 主控 → 继任者（原计划收口）

- **交出时 HEAD**：以当时分支最新提交为准（T3.2/T3.3 为 `063f1bc`；计划外清晰度 D-18/D-19 为 `fb206c5` / `ee8a527`）。本条只关闭 **03 文档 Phase 3 原计划**，不把工作区里未提交的 Phase 4 半成品算进 Phase 3。
- **原计划四项**：
  - T3.1 打包 49/49：`c849707`
  - T3.2 导入：跳过 book-001，48/48 成功；阅读表未动：`063f1bc`
  - T3.3 只读五类断言 49/49：`063f1bc`
  - T3.4 真人抽查：**2026-08-18 用户确认「已经抽查过，效果合格」**，按 L4 记
- **明确划到计划外、不挡 Phase 3 收口**：原版 PDF 发糊（D-18 选尺 / D-19 字号与舞台）。用户原话「清晰度的问题我在另一个地方解决了。这个是计划之外衍生出来的问题。」
- **本任没做的**：Phase 4/5/6/7。工作树若已有发布管理/受保护资产等改动，那是别的会话，不是本任 Phase 3 产出。
- **book-001**：仍不重导。阅读器必须覆盖它（T3.4 抽查含该书），但重导禁令未解除。
- **给继任者**：先读本文件第 10 节上一则（D-19 → Phase 4）和 `06_Phase4交接说明.md` 里的四处更正，再动 Phase 4。不要从 03 旧进度「下一步 T3.2」开干——那行已过时。

### 2026-08-18 · Phase 8 产品裁决入口（只追加）

- Phase 8 当前唯一产品与实施权威入口为 `09_Phase8班级管理系统设计与交接.md`；逐条裁决留痕在 `evidence/phase8/decisions.md`。
- `02`、`03`、`04` 与 `evidence/phase4/decisions.md` 中“教师全局发布”“无 grants=全组织可见”“可见范围复用 book.publish”均为历史口径，已由 Phase 8 覆盖，不得直接交给执行者照做。
- 新增 D-25：`book.publish` 耦合全局书库生命周期与班级书架写入。Phase 8 必须拆动作，但复用 Phase 4 的表、共享谓词、scope 推导、F-1 对称校验、幂等与审计。
- 产品已裁决教师默认可信：教师专用凭据完成入口校验后，自选本校班级立即生效，不设教师归属审批；技术团队、校长、年级主任都可签发教师凭据。

### 2026-08-18 · Phase 8 主控 · T8.0/T8.1（只追加）

- 分支 `feat/product-close-loop`，HEAD `b3cd4b5`，迁移最大号 046。真库只读复核仍满足 09 §11.1。
- T8.1 独立复核完成。B/C 契约缺口已落入 `09` §10.4 与 `evidence/phase8/decisions.md` P8-16～P8-23。无 D。
- **不可进入代码阶段**：等用户批准 Gate 1；Phase 6 未归档，T8.2 及之后必须重核基线。
- 5191 已被既有 `node server/index.js` 占用。本轮未写业务代码、未开浏览器、未重启后端。

### 2026-08-18 · Phase 8 主控 · 顺序纠正与 Gate 1 批准（只追加）

- 用户纠正：Phase 8 先实施并完成，Phase 6 再做阅读计时联动验收。Phase 6 未归档不阻塞 T8.2。
- Gate 1 已批准。leave_self 幂等见 P8-17R；POST /students 唯一处置见 P8-18R；T8.2 拥有 047～050。
- 本次批准不含 T8.9、重启共享 5191、浏览器、Phase 6 正式验收。下一步 T8.2，做到 Gate 2 停止。

### 2026-08-18 · Phase 8 主控收口（只追加）

- **交出时 HEAD**：以本条提交后的检查点为准（收口前工作区未提交）。基线仍从 `b3cd4b5` 分出。
- **已完成**：T8.2–T8.9 verified；Gate 1 用户批准；Gate 2 质量门+副本演练；Gate 3 真库 047–050 + 独立观察。用户称 T8.10 浏览器验收暂时没有问题。
- **现网**：共享 5191 已是新代码（health `migrations:34`）。回退资格已变（窗口内写入验收二班与新学生）。不得用 TEMP 旧备份覆盖真库。
- **质量门（收口亲自跑）**：`test:server` 437/437，`test:frontend` 270/270，`build` 退出码 0。
- **明确没做 / 不得宣称**：`04` G1–G20 未逐条 L4；真网 draft+grant 租约未打；真库无 `grade_manager`（bootstrap 未在正式切换时跑）；未 commit 前不要当已入库；**未 push、未开 PR、未开 Phase 6**。
- **登录**：`{schoolCode, loginName, password}`。学校码 `internal-demo`。展示名可中文；登录名仍是 ASCII 唯一键，不是幂等 upsert。
- **红线未变**：不改两张阅读摘要表、session-summaries schema/指纹、90s TTL、renew；agent 不开浏览器。
- **给继任者**：先读 `09` 与 `evidence/phase8/close-report.md`。下一阶段是 Phase 6，须用户另授权。不要从本文件更早条目「下一步 T8.2 / 不得进 T8.9」开干——那两句已被本条覆盖。

### 2026-08-18 · Phase 8 检查点已入库（只追加）

- 上一条写「交出时 HEAD 以本条提交后的检查点为准（收口前工作区未提交）」以及「未 commit 前不要当已入库」：那是收口当时的状态，现已被本条覆盖。
- **交出时 HEAD**：`e8cbe4e32ecc8c21d4e81f99ebd84154f35e8a64`（短哈希 `e8cbe4e`）。提交说明：`feat(close-loop): Phase 8 班级管理、默认全闭与 047-050 正式切换`。基线仍从 `b3cd4b5` 分出。
- 第 1 节检查点表仍停在 `c40765a`，质量门数字仍写 207/179。那是 Phase 2 当时基线，不要改表；Phase 8 收口实跑是 server **437/437**、frontend **270/270**、`build` 退出码 0。
- **未纳入本检查点**：`.trellis/`（含 OCR 任务归档，与 Phase 8 无关）；`.env`；真库 `server/data/readmate.sqlite`；`dist/`。
- **仍未做**：push、PR、Phase 6。现网 5191 未在本条重启或替换。

### 2026-08-19 · Phase 6 主控收口（只追加）

- **交出时状态**：Phase 6（双模式 × 阅读计时联动验收）机器阶段（Gate A）与真人运行阶段（Gate B）已全部完成并通过验收。
- **质量门实跑基线**：`test:server` **445/445**，`test:frontend` **283/283**，`npm run build` 退出码 **0**。
- **完成内容**：
  1. T6.0 基线核对、T6-Guard 补齐 13 个双模式/会话/生命周期/D-23 隔离守卫；
  2. T6-Verify 独立机器验证：独立端口 5199 + 临时隔离 SQLite 实例真实 HTTP 链路验证通过，Gate A **PASS**；
  3. Gate B 真人运行：用户在真实浏览器中完成单连续路线阅读、双模式切换、后台暂停恢复、退出重登与 5 分钟打卡；真库只读采样确认新会话正常 closed，逐页 coverage 分账与日汇总增量精确；
  4. 解决用户发现的控制台交互缺陷（T6-Fix）：`useReadingStatistics.js` 中引入班级自然数字序排序算法（一班排在二班前），并引入基于 `workspaceId` 隔离的 `localStorage` 选班记忆；补齐 8 项单测，独立验证通过；
  5. 明确阅读行为指标口径：确认系统中仅有跳读（hadSkip，相邻翻页停留<5s且连续2次）与回读（hadReread，回退跨度>=3页且目标页停留>30s）两类行为。
- **冻结红线遵从**：
  - 严禁修改的两张阅读摘要表、session-summaries schema/指纹算法、90s TTL、renew 路由、唯一 coordinator、物理页语义等全部严格保持冻结（零改动）；
  - 真库约 194544ms 历史僵尸数据未做任何清理或删除，全流程按增量（Δ）口径成功闭环。
- **明确未做**：不进入 Phase 7（端到端最终验收与报告），未索取真实 AI 凭据，未 push，未建 PR。
- **给继任者**：先读 `evidence/phase6/execution-ledger.md` 与 `evidence/phase6/close-report.md`。下一个阶段是 Phase 7（需用户单独授权并提供 AI 凭据）。

