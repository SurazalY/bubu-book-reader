# 原版 PDF 演示选尺（D-18）

只改学生阅读器怎么量尺子，不重导书、不重渲染 PDF。

- **抽样**：PDF ready 后量第 1、2、中间、末页（1-based，去重；页少就少抽）。
- **选尺**：对宽、高分别取下中位数（偶数样本取排序后中间两个里偏小的），避免封面 MediaBox 把全书放大。空抽样直接报错，不回退封面。
- **消费**：原版仍把 `pdf.width/height` 当 `pageDesign`；文字模式仍用 `PAGE_DESIGN`（468×636）。每页画进同一 design 框，scale 仍夹在 0.4–2。
