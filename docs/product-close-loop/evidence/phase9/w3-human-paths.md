# W3 真人路径验证记录

- 日期：2026-08-20
- 前端：`http://127.0.0.1:5190`
- 后端：`http://127.0.0.1:5191`（验收中途重启过一次，见下）
- 账号：教师 `internal-teacher-li`；学生以当时重置的目标为准（林小竹 / kay / yak2）
- 口令来源：仓库根 `.env` 的 `INTERNAL_DEMO_PASSWORD`；重置后以教师端看到的 6 位为准（本文不写原文）
- 浏览器：产品负责人本人操作。不派 AI 代点。无截图（负责人明确说无需截图）。

---

## 路径 1 · 教师重置后看到 6 位密码

**PASS**（产品负责人当场确认）

控制台对学生点「重置密码」，看到一串 6 位密码。

过程插曲：第一次整表「临时密码」列与签发按钮都返回「资源不存在」。根因是旧后端进程（2026-08-19T15:07Z 启动、无 `--watch`）未加载 T3-2 路由。重启 `npm run server` 后 health `migrations: 37`（含 053），未登录打新接口为 401 而不再是 404。另：页面上方「目标账号」曾把登录名原样塞进 `/users/:id/`；新进程上会变成「账号不存在」。已改为先在当前名单解析 UUID。

## 路径 2 · 学生用新密码登录，旧密码失效

**PASS**（产品负责人确认「可以重置密码，并且重置后的密码生效了」）

## 路径 3 · 老师再回来仍能看见（不是只显示一次）

**PASS**（产品负责人确认「这两条都过了」）

## 路径 4 · 学生自助改密后老师看不到明文

**PASS**（产品负责人确认「这两条都过了」）

学生设置改密后，教师端该学生显示「已自行修改」，不再显示明文。

---

## 本波次不验 / 已识别但不作为出口

- 学生登录页「忘记密码」说明页（无重置码输入框）：出口未要求，未单独打回。
- 表格「学生」列实际已是 `displayName`。kay / yak2 看起来像登录号，是因为他们的展示名就是这串。按负责人要求加了「展示名」列。`GET /students` 不下发 `loginName`，登录号列未加。
