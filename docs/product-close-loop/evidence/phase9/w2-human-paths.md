# W2 真人路径验证记录

- 日期：2026-08-19
- 前端：`http://127.0.0.1:5190`
- 后端：`http://127.0.0.1:5191`（验收中途重启过一次，见下）
- 账号：学生 `internal-student`、教师 `internal-teacher-li` / `internal-teacher-wang`、校长 `internal-principal`
- 口令来源：仓库根 `.env` 的 `INTERNAL_DEMO_PASSWORD`（本文不写原文）
- 浏览器：产品负责人本人操作。不派 AI 代点。无截图（负责人明确说无需截图）。

---

## 路径 1 · 学生改密且当前会话仍有效

**PASS**（产品负责人当场确认）

- 个人主页 → 设置 → 旧密码 + 两次新密码 → 修改成功，新密码立即生效。
- 改密后**没有被踢回登录页**，仍停留在登录态。

过程插曲：第一次改密报「受保护请求必须携带 X-Workspace-Id」。根因是旧后端进程（约 20:35 启动、无 `--watch`）未加载 T6-2 路由。重启 `npm run server` 后复验通过。不是接口契约要工作空间头。

---

## 路径 2 · 教师 / 校长改密与改显示名

**PASS**（产品负责人当场确认）

- 教师能改密码，且生效。
- 校长能改密码，且生效。
- 显示名可以改并保存成功（负责人确认「这两点都成功了」，含改显示名与改密后会话保留）。

---

## 本波次不验

契约第五节第 4 条后半「老师端该学生的临时密码显示为已自行修改」属于 **W3**，表 `issued_temp_passwords` 尚未创建。
