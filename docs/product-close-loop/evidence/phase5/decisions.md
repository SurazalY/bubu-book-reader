# Phase 5 决策记录

## P5-01 第一批真人验收（2026-08-18）

用户本人在本机浏览器执行。前端曾因 Vite 进程消失出现 `ERR_CONNECTION_REFUSED`，主控只重启了 `npm run dev`（5190），未重启后端 5191。

| 项 | 五级口径 | 用户原话 / 证据 |
|---|---|---|
| T5.2 空白页占位 | 实际运行通过 | 「5.2验收成功」；book-001 文字模式空白页 |
| D-06 页内纵向滚动 | 实际运行通过（有已知偏差） | 见 P5-02 |
| T5.3 跳页参数 | 实际运行通过 | 「5.3验收成功」 |
| T5.4 学生端退出 | 实际运行通过 | 「5.4验收成功」 |
| T5.5 印刷页标签 | 实际运行通过 | 「5.5验收成功」；预期无需改动，未改代码 |

机器质量门（独立验证方，未改文件）：`test:server` 258/0，`test:frontend` 240/0，`build` 退出码 0。

不把 D-09 / D-10 标成「真人复验通过」。本窗口未专门探测冷缓存详情封面、取消发布后旧缓存是否仍可读源 PDF。

## P5-02 D-06 鼠标上下拖不能滚（已知偏差，不改修法）

用户：「d06唯一的问题是鼠标上下拖动是不行的，但是可能是兼容性问题，放在平板上可能可以但现在没条件试。」

- **不**改已裁决的「页内纵向滚动」，不改成缩字号或展开。
- 不因此阻塞 T5.1。
- 平板触摸未验，记「因环境尚未验证」。
- 实施方代码事实：`useReaderGesture` 无 wheel 监听；翻页只认横向主导位移。鼠标按住上下拖可能被手势栈当成 tap / 未交给 `overflow-y: auto`。未改 `useReaderGesture`。

## P5-03 迁移号 030 与仓库冲突 → 用户允许改用 046

08 / 09 初稿写「T5.1 取 030，Phase 8 从 031 起」，并写「不必再协调」。**前任交接没有核 `server/db/migrations/`。** 实测目录里已有：

- `030_community_reports_delivery.sql`（社区举报投递，早已占用）
- 其后 031–033、040–045
- 当前最大号 **045**
- Phase 4 的 `06` 更正二其实已经写过「新迁移从 046 起」

若仍落 030，会撞社区举报表，`schema_migrations` checksum 会炸。2026-08-18 本阶段主控向用户报告后，**用户明确允许改用 046**。Phase 8 从 **047** 起。08 / 09 / 本条已改判。不调到 046 会出问题。

## P5-04 投影守卫已收下（2026-08-18）

独立守卫只写测试、未改实现：

- `tests/server/http/books-projection-snapshot-guard.test.js`
- `tests/frontend/reading-fingerprint-snapshot-guard.test.mjs`

HTTP 冻住 `GET /books` 顶层 14 个 key，允许新增、禁止缺失/改类型。计时三表列精确相等（加列也红）。`FINGERPRINT_FIELDS` 按源码冻 **15** 个（简报口误写成 16，以源码为准）。守卫自称 `test:server` 261。

**给 T5.1 的硬约束（不改这道守卫）：** `projectBooks` 源码扫描禁止出现 `readerMode` / `reader_mode`。B-3 允许 `GET /books` **新增**字段，但字段名不能叫这两个，否则守卫红、按实施越界处理。表列与 PUT body 仍用契约里的 `mode`。投影新增字段用 `preferredReaderMode`。

## P5-05 T5.1 代码已落地，待重启与真人验收（2026-08-18）

实施方按 046 + `preferredReaderMode` 落地。抽查：新表五列、PUT 只写会话用户、投影只新增字段、Reader 保留默认 `useState` 后用偏好覆盖、切换 fire-and-forget、无端上存储。两份守卫未改。`reading-monitor-migration.test.js` 只把「末条迁移名」从 045 改到 046，计时表断言未动。实施方自称 server 264、frontend 244。

未重启 5191，业务库尚未跑 046。不宣称验收通过。

## P5-06 T5.1 真人验收 + 阶段收口口径（2026-08-18）

用户原话「验证通过」（刷新与重登后模式恢复）。C5/C6 标 L3。健康检查 `migrations: 30`。

用户问「Phase 5 是不是验收完了」。口径：

- T5.1–T5.5 真人窗口**齐了**（D-06 带已知偏差）。
- **不要**写「Phase 5 验收通过」总章。04 里 A/D/E/F 与未点到的 C 项不在本窗口。
- Phase 4 归档已按 08 §四写完。提交仍须另问用户。

## P5-07 收口质量门独立复跑（2026-08-18）

独立验证方只跑命令、未改源码：`test:server` 264/0，`test:frontend` 244/0，`build` 退出码 0。条数相对阶段初只升不降。`build` 有 vendor-icons / StudentApp / pdf.worker 超过 500kB 的警告，不记失败。不宣称验收通过。
