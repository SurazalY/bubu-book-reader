# Phase 5 交接说明（Reader 补缺 · 限定范围版）

写给接手 Phase 5 的**主控**。写作时间 2026-08-18 上午，由上一任主控（Phase 4）交出，额度将尽。

**本阶段被用户明确限定了范围，不是完整的 Phase 5。** 请先读完第一节再动手。

---

## 一、本阶段做什么、不做什么

### 已经完成（在你接手之前，不要重做）

- **Phase 4 真人验收已通过**（2026-08-18 上午，用户本人执行）：教师下架《鲁滨逊漂流记》→ 学生书架消失 → 再发布 → 恢复，四步全对。
- **已提交一次**：`547164d`，49 个文件 / +7931 行，是 Phase 4 的干净基线（业务库与 `dist` 在忽略清单内，未入库）。

### 要做

| 顺序 | 内容 | 性质 |
|---|---|---|
| 1 | T5.3 跳页参数修复 | 纯前端，最小 |
| 1 | **T5.2 空白页占位 + D-06 文字模式内容不可达（合并为一个任务）** | 纯前端，本阶段最实质的一条 |
| 1 | T5.4 学生端退出登录 | 纯前端 |
| 2 | T5.5 印刷页标签检查 | 验证，**用户本人在浏览器里做** |
| 3 | **T5.1 模式持久化** | **唯一带迁移与后端重启的一条，必须压在最后** |
| 4 | Phase 4 归档 | 纯文档 |

第 1 组三条任务**互不依赖、可并行派发**，都不碰服务端、不需要迁移、不需要重启后端。前端 Vite 有热更新，改前端不影响用户同时在浏览器里看效果。

**T5.2 与 D-06 必须合并成一个任务派给同一个子 agent**，因为两者改的是同一块渲染代码（`BookPage.jsx` + `index.css`）。分两次做，第二次几乎必然要推翻第一次的布局决定。

### T5.1 为什么压在最后（2026-08-18 改判，此前一度冻结）

**它是本阶段唯一要跑迁移、要重启后端的任务。** Node 不热加载，而后端 5191 与用户在浏览器里看效果用的是同一个实例。压到最后，重启只发生一次，且发生在前三条都验完之后，不打断任何事。若按原编号打头做，整个阶段都要在"随时可能被重启"的环境里验前端。

**它也不能再往后拖到 Phase 8 之后**，这是本次改判的实质原因：T5.1 的完成标准含"**重登恢复**"，而 Phase 8 要改的恰恰是学生怎么登录进系统（自助注册 + 选班 + 审核队列），且 `internal-demo-student` 现在是 bootstrap 直接造的、班级关系直写 `class_memberships`、没走过审核，Phase 8 很可能要重跑 bootstrap 或写数据迁移（见 09 文档 §五 风险四）。**现在登录链路刚被用户亲手验过是好的，是验 T5.1 最干净的窗口**；放到 Phase 8 之后，验"重登恢复"会和一套全新的注册审核流缠在一起，分不清"没恢复"是偏好没存住还是登录态变了。

**修法不得简化为端上存储。** D8 明确规定恢复优先级是"服务端偏好 → 默认逻辑"，B-3 规定新表 `reader_mode_preferences`（org / user / book_version_id / mode / updated_at，唯一键 user + book_version）+ `PUT /reading/reader-preference`，写请求带 Idempotency-Key，学生本人权限。除契约原因外还有产品原因：**学校平板是共享设备，偏好存在浏览器端会串到下一个登录的学生身上**。

**迁移号（2026-08-18 改判）：T5.1 取 046，Phase 8 从 047 起连续取号。**

> **号段矛盾（请后来者先读这一段）。** 上一任主控在本文件初稿里写「T5.1 取 030，Phase 8 从 031 起」，09 文档同步写了同一句话，并且写「不必再协调」。**那次没有核对 `server/db/migrations/` 目录。** 实测：`030_community_reports_delivery.sql` 早已存在，其后还有 031–033、040–045；Phase 4 的 `06` 文档更正二也已经写过「新迁移从 046 起」。若仍按 030 落盘，会覆盖/撞上社区举报表，迁移校验和 checksum 都会炸。2026-08-18 本阶段主控抽查目录后向用户报告，**用户当场允许改用 046**。08/09 初稿里的 030/031 作废。不调到 046 会出问题。

**两条禁令**（B-3 原文）：不得把 `readerMode` 塞进计时摘要、`reading_progress` 写路径或任何冻结 DTO；`GET /books` 投影可以**新增**字段携带偏好，**新增允许，改动或删除已有字段不允许**。

**关于模型档位（2026-08-18 改判）。** 03 与 05 文档把 T5.1 点名为高风险设计类、要求 opus 级模型。**本阶段改为不用 opus**，用户要求尽量减少对其依赖。改判依据：T5.1 当初被标高风险，标的是*设计*风险——表结构、端点语义、会不会污染冻结的计时摘要契约；而这些现在已由 B-3 写死（表名、五个字段、唯一键、端点路径、Idempotency-Key、投影只许新增字段），**设计已不在子 agent 手里，剩下的是纪律问题**。

纪律问题用机械闸门解，比用更强模型解更可靠也更便宜。Phase 4 有直接证据：该阶段最危险的缺陷 F-1（班级级教师用 `scope=organization` 绕过班级授权、反向覆盖全组织授权）**是独立的对抗式守卫 agent 攻出来的，不是靠模型档位避免的**——写实现的 agent 自己没发现，主控抽查也没发现。D-10 那次子 agent 留下 `max-age=3600` 默认分支、把缺陷改成休眠状态，抓住它的同样是人读源码。

**所以 T5.1 降级的对价是一道闸门，必须做，不是可选项：**

在 T5.1 开工前，**先派一个独立 agent** 写投影字段快照守卫——固定当前 `GET /books` 返回的字段集合，任何已有字段被改名、改类型或消失即红；同时断言计时摘要相关表与 DTO 未被触碰。**这道测试由别人写，T5.1 的实施 agent 不得修改它**，这条要写进派发简报。若 T5.1 落地后该守卫变红，一律按"实施越界"处理，不得通过改测试来消红（Phase 4 曾出现过必须改守卫测试的情形，但那是主控逐条裁决并记录在 `evidence/phase4/decisions.md` 的例外，不是默认路径）。

**档位：本阶段全部五次派发（T5.3、T5.4、T5.2+D-06、投影守卫、T5.1）一律用 `cursor-grok-4.6-xhigh-fast`。** 不要为省钱给 T5.3 这种小活挑更弱的档——用户已说明 grok 的实际成本低于 gemini，降档没有收益。**`composer-2.5-fast` 永远不启用**（用户判定能力不足，硬禁令）。完整惯例见 05 文档硬规则第 7 条。注意 05 文档早期写的 `cursor-grok-4.6-high-fast` 在当前环境已不存在，模型列表会变，派发前先确认。

### 不属于本阶段

Phase 8（班级管理系统 + 可见范围默认翻转）见 `09_Phase8班级管理系统设计与交接.md`。用户的计划是：本阶段做完 → 再进 Phase 8。**本阶段不要碰** `server/domains/identity/`、`server/domains/reading/visibility.js`、`src/console/pages/accounts/`、可见范围相关的任何东西。

---

## 二、三条任务的落点与完成标准

原始定义见 `03_实施任务清单.md` §Phase 5。以下是补充的落点与坑。

### T5.3 跳页参数修复（最小，建议先派）

`src/student/pages/Highlights.jsx` 用的查询参数是 `?page=`，而阅读器的 `resolveReaderLocation` 认的是 `?pageNo=`，导致**从摘录跳转会落到错误的页**。改成 `?pageNo=`，并**全局搜索其他 `?page=` 误用处**一并修。要有测试覆盖。

### T5.2 空白页占位

文字模式下，对"导入时标记为空页"的页（无 block、normalized 文本为空）渲染明确的空白页占位（如"本页为空白页"），**与网络/加载错误状态视觉上要能区分**。PDF 模式不变，空白页不影响翻页与页号。更新前端契约测试。

**可验证的真实数据**：`book-001`《和大人一起读·儿童歌谣》98 页里有 10 个空白页（如 page-0002、page-0003）。

**本条已与 D-06 合并**，同一个子 agent 一次改完这块渲染层，见下一小节。空白页分支仍应写成结构独立的渲染分支（"没有内容时显示什么"与"内容太多时怎么让用户读到"是两件事），但不必再为跨任务的兼容做额外防御。

### D-06 文字模式长页正文被裁掉且不可达（与 T5.2 合并）

**这是内容丢失，不是显示瑕疵**，影响 49 本里所有文字较多的页。

根因已查清，不必重查：文字页是固定 468×636 的设计画布，窗口变化只做 `transform: scale`、**禁止重排**（`src/student/components/BookPage.jsx` 第 6–14 行）。裁切发生在多层且**均无 `overflow-y: auto`**：`.student-page-frame` 的 `overflow: hidden`（`src/index.css` 1228–1230）、`.student-page-body` 的 `min-height:0; overflow: hidden`（同文件 1308–1314）。页内不能滚、不能展开、翻页是换到另一张物理页。切回原版 PDF 能看全文，但那是换模式，不构成文字模式的阅读能力。

**修法已裁决：页内纵向滚动。** 即放开那几层 `overflow: hidden`、给正文容器加 `overflow-y: auto`，保留固定画布与等比缩放的整体设计。

选它的三条理由：一是**代码里已有先例**——D-19 的修复给原版模式的"大"字号档做的正是"放大到 1.6 倍 + 纵向滚动"，文字模式沿用同一套交互模式，一致且有参考实现可循。二是不改变页号语义（一张物理页仍是一页，滚动只是把这一页看完）。三是另一个候选"自动缩小字号以适配"方向是错的——D-19 已经量化证明这批扫描件正文汉字字身仅 8.7–9.9 pt，按窗口铺满时只有约 11 设备像素，已在汉字可读下沿，再缩会更糟。

**两个必须注意的点**：滚动手势与翻页手势可能冲突（翻页效果当前默认是 `slide`，`curl` 分支已按 D-03 裁决置为不可达），要确认页内滚动不会误触翻页、也不会被翻页容器吞掉；顶栏底栏收起时的 flex 流处理已由 D-19 改过，不要回退它。

**若实施中发现页内滚动会破坏翻页或其它既有交互，停下报告，不要自行改用别的修法**——修法是产品裁决。

### T5.4 学生端退出登录

个人中心 `/student/me` 加"退出登录"入口 → 调已有的 `POST /auth/logout` → 跳 `/student/login`。

**不要在 logout 里加任何计时处理。** 从 Reader 返回再退出的路径上，计时会话已由现有逻辑按 `reader_close` 收尾。**阅读计时是项目冻结契约**（表、schema、指纹算法、租约 TTL、续租路由一律不动），它在 D-11 / D-15 / D-17 三次复发过，是本项目最大的雷区。

**一个与 Phase 8 的轻度重叠**：Phase 8 要在学生端显示"我在哪个班"，很可能也落在 `/student/me`。冲突是文本级的（两块不相关的 UI 加在同一页），不构成阻塞，但请在本阶段完成后于本文件末尾记一行，方便 Phase 8 知道该页已被改过。

### T5.5 印刷页标签检查（验证任务，放进验收窗口）

本批书 `printedPageLabel` 全为 null，确认底栏"第 X 页 / 共 Y 页"的物理页语义正常。**预期无需改动，异常才修。**

---

## 三、已裁决事项一览（不要再问，也不要自行更改）

| 事项 | 裁决 | 时间 |
|---|---|---|
| D-06 修法 | **页内纵向滚动**（理由见 §二） | 2026-08-18 |
| D-06 时机 | 本阶段做，与 T5.2 合并成一个任务 | 2026-08-18 |
| T5.1 时机 | ~~排在 Phase 8 之后~~ → **改判：拉回本阶段，压在最后一步**（理由见 §二） | 2026-08-18 |
| T5.1 修法 | 按 D8/B-3 原样落地（服务端新表 + 端点），**不得改为端上存储** | 2026-08-18 |
| 模型档位 | **本阶段不用 opus**（含 T5.1，推翻 03/05 文档的要求）；对价是 T5.1 前必须先由独立 agent 写投影字段快照守卫 | 2026-08-18 |
| Phase 4 跨班隔离真人验收 | 不做，存档为"机器已验、真人未验"，留给 Phase 8 补验 | 2026-08-18 |
| 提交时机 | 已完成（`547164d`） | 2026-08-18 |
| 迁移号 | ~~T5.1 取 030，Phase 8 从 031 起~~ → **改判：T5.1 取 046，Phase 8 从 047 起**。原因：前任交接未核目录，030 已被 `030_community_reports_delivery.sql` 占用；用户允许改号。详见上文号段矛盾 | 2026-08-18 |

若实施中发现某条裁决落地会破坏既有功能，**停下报告，不要自行改方案**。

---

## 四、Phase 4 收尾（用户已指定由你一并完成）

完整剧本、账号口令、已验证到哪一级、残余缺陷，全部在 **`07_Phase4收尾与交接.md`**，**请完整读一遍**。这里只讲用户已经做出的三个决定：

1. **Phase 4 收尾归你**（真人验收 + 归档）。
2. **跨班隔离的真人验收不做**。原计划要在业务库里手工建第二个班（浏览器里建不了，见 D-24），用户裁决**跳过**——理由是 Phase 8 要重建整套班级管理（校长建班会做成真界面）且要翻转可见范围默认值，现有语义会被替换，为验收专门手工写一次库收益太低。**该验收项按「机器已验、真人未验」存档，留给 Phase 8 用真实建班界面一并验。** 不得写"通过"。
3. **年级主任那条线做不了**（演示数据里没有 `grade_manager` 账号也没有年级工作空间），同样不得标真人验收通过。Phase 8 会补这个账号。

所以实际需要真人验收的只有一项——而它**已于 2026-08-18 上午由用户本人验过并通过**：下架《鲁滨逊漂流记》→ 学生书架消失 → 再发布 → 恢复，四步全对。**你不需要再推动任何真人验收，只需完成下面的归档。**

**归档时请注意一处口径**：这次验收的四步覆盖了学生书架渲染与打开阅读，因此 D-09（详情页封面）与 D-10（资产缓存）的修复路径被**顺带走过**，但**没有专门探测**它们各自的关键场景（冷缓存下的详情页封面、以及取消发布后旧缓存是否仍可读到源 PDF）。台账里这两条不要直接改成"真人复验通过"，应记为"主链路已验、专项场景未单独探测"。

### 归档清单

- `04_端到端验收清单.md` 里 Phase 4 相关项填五级状态与证据。**未真实运行的项不得写"通过"**（项目硬规则）。
- `06_Phase4交接说明.md` 追加两处已知失准：它说"无 `class_teacher` 角色、不需处理别名"，实际 `permissions.js` 有 `roleAliases` 表（`class_teacher: 'teacher'`、`grade_group: 'grade_manager'`）；它说 D-18/D-19 的改动未提交，实际 `git log` 显示已提交。
- 台账 `evidence/phase2/defect-ledger.md`：D-09/D-10 现为"已修待复验"，真人复验通过后才能改状态。
- 决策记录写进 `evidence/phase4/decisions.md`（已有 36KB，Phase 4 全部决策在内）。

---

## 五、提交状态

**Phase 4 的提交已完成**：`547164d`，49 个文件 / +7931 行，分支 `feat/product-close-loop`。业务库 `server/data/readmate.sqlite` 与 `dist/` 在忽略清单内未入库；`.trellis/` 与 `AGENTS.md` 属别的任务的产物，有意留在未跟踪状态。

**你自己的改动怎么处理**：项目原有硬规则是"不推不提交"。Phase 4 那次是用户明确授权的例外。**本阶段完成后是否提交，请向用户确认**——不要自行决定。无论如何**都不要 push**。

---

## 六、继承的硬规则与红线

**逐条都是用户定的红线，违反即事故。**

- **主控不写业务代码、不跑测试构建、不直接探索代码库。** 一切实现派子 agent。主控可读文档、做最小抽查。
- **不开浏览器**，不使用任何浏览器工具。浏览器操作全部由用户本人执行。本项目曾因 agent 开的标签页脱管、持续占用业务资源而堵死近三小时。
- **不得宣称"验收通过"**，必须有真实运行证据。五级状态：代码已实现 / 自动测试通过 / 实际运行通过 / 真实书目验证通过 / 因环境或凭据尚未验证。
- **阅读计时是冻结契约**：表、schema、指纹算法、租约 TTL、续租路由一律不动。
- **不得向业务数据库 `server/data/readmate.sqlite` 手工写入。** 测试一律用临时库（`mkdtempSync`）。
- **决策必须留痕**。
- **除第五节授权的那一次提交外，不得做其它 git 写操作**，尤其不得 `git checkout` / `reset` / `stash` / `clean` / `restore`——工作区里堆着多个 agent 的未提交成果，清掉找不回来。

---

## 七、质量门基线与环境

**当前基线（2026-08-18 10:25 实跑，全绿）**：

| 门 | 数字 |
|---|---|
| `npm run test:server` | 258 / 258 pass / 0 fail |
| `npm run test:frontend` | 228 / 228 pass / 0 fail |
| `npm run build` | 退出码 0 |

**你的任务只应让 frontend 的数字上升，server 应保持 258。** 测试条数变少一律视为异常。

- 前端 Vite 在 **5190**（PID 11020），后端在 **5191**（PID 51112，已于 10:25:57 重启，加载了最新代码）。
- **后端没有热重载**，改服务端代码必须重启。但本阶段三条任务都是纯前端，**不需要重启后端**——这正是它们能和用户的浏览器验收并行的原因。
- **本项目前后端依赖分开安装**：根 `package.json` 管前端，`server/package.json` 管后端（`express`、`dotenv`，都已装在 `server/node_modules`）。判断依赖缺失必须逐包检查，不能只看仓库根——上一轮有 agent 因此报了个【高】级误报。
- **npm 11 会吞掉 `--` 之后的参数**，单跑一个测试文件要直接 `node --test <路径>`。
- 演示账号五个共用口令 `ReadMateDemo#2026Kx9`，账号清单见 `07_Phase4收尾与交接.md` §二。

---

## 八、派子 agent 的经验（Phase 4 实打实的教训，请照用）

- **每个 prompt 里必须重申完整禁令**，尤其"不得 `git checkout`/`reset`/`stash`/`clean`"。
- **要求每份报告分「实测」与「推断」两栏**。这一条在 Phase 4 抓出了大量问题，是性价比最高的一条要求。
- **子 agent 会越界做"顺手的清理"**。有一个以"不得出现完成度"为由删掉了既有 UI 里的阅读安排完成率（那是班级管理指标，不是个人阅读进度，且契约限定的是**新增**界面）。已驳回并还原。**硬契约是约束新增的，不是授权在无关范围内做清理。**
- **子 agent 会留下"休眠缺陷"**。D-10 第一版把封面改成 `no-store` 但其余资产类型保留 `max-age=3600`，等于把缺陷改成潜伏态。要求改成 fail-safe 默认后才通过。
- **子 agent 的高危报告也可能是误报**，要自己核。
- **改适配器层字段前必须把消费方引用链查到底**，不能靠组件名和跳转路径推断归属。上一任主控曾误判 `src/components/ui.jsx` 的 `BookCard` 在学生端路径上，复核后发现它只服务旧站演示数据，及时中断了任务。
- 独立验证方（只写测试、不改实现）的测试文件是它的资产，实施方不得随意改动；需要改时逐条授权并记录理由。新增用例放新文件。

---

## 九、本阶段完成后请补记

- **T5.4**：`/student/me`（`src/student/pages/Me.jsx` 左栏身份卡底部）已加「退出登录」，走 `POST /auth/logout` 后 `replace` 到 `/student/login`。Phase 8 若在同页加「我在哪个班」，这是文本级并存，不是冲突。
- **质量门（收口独立复跑，2026-08-18）**：`test:server` **264 / 0**，`test:frontend` **244 / 0**，`build` 退出码 **0**（约 7.79s；有 chunk >500kB 警告，不是失败）。相对阶段初 258 / 228 只升不降。与 T5.1 实施方自称一致。不据此写「验收通过」。
- **Phase 4 真人验收**：下架往返 L3/L4（2026-08-18 上午，《鲁滨逊漂流记》）。跨班隔离机器已验、真人未验。年级主任线未验。证据：`evidence/phase4/decisions.md` 第八节、`evidence/phase5/decisions.md`、`04_端到端验收清单.md` 本轮填写。
- **提交**：Phase 4 基线 `547164d`。本阶段改动尚未提交。是否提交须再问用户；不要 push。
